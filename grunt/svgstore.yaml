# SVG-Store
# https://github.com/FWeinb/grunt-svgstore
# 2014-12-12 - DEPRECATED, we use "svgstylus" instead !!!
options:
  prefix: 'svg-'
  cleanup: false
  svg:
    style: 'dislay:none;'

production:
  src: '<%= lmConf.svg_src %>'
  dest: '<%= lmConf.svg_dist_file %>'
