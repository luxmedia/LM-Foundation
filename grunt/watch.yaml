# main watch tasks
stylesheets:
  files: '<%= base_paths.src  %>/<%= base_paths.distCss %>/*.styl'
  tasks: 'stylesheets'

coffee:
  files: '<%= base_paths.src  %>/**/*.coffee'
  tasks: 'coffee'

scripts:
  files: 
    - '<%= base_paths.src  %>/js/**/*.js'
    - '<%= lm_conf.js_dist_folder %>/*.js'
  tasks: 'scripts'

svgs:
  files: '<%= lm_conf.svg_src %>'
  tasks: 'svgs'

jade:
  files: '<%= base_paths.src  %>/jade/*.jade'
  tasks: 'jade'

copy:
  files:
    - '<%= base_paths.src  %>/**'
    - '<%= lm_conf.do_not_copy %>'
  tasks: 'copy'