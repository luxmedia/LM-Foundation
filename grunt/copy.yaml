# Copy files to dist directory
dist:
  files:
    -
      expand: true
      cwd: '<%= base_paths.src %>'
      src:
        - '**/*'
        - '!**/*.styl'
        - '!**/*.coffee'
        - '!**/*.jade'
        - '!**/*.svg'
        - '!**/*.json'
        - '!json'
      dest: '<%= base_paths.dest %>'
      flatten: false
      filter: 'isFile'