// Foundation by ZURB
// foundation.zurb.com
// Licensed under MIT Open Source

@require '_global'

//
// @variables
//

$include-html-sub-nav-classes =              com_css.SUBNAV

// margin and padding
$sub-nav-list-margin ?=                      rem-calc(-4 0 18)
$sub-nav-list-padding-top ?=                 rem-calc(4)

// the definition
$sub-nav-font-family ?=                      $font-family-custom
$sub-nav-font-size ?=                        rem-calc(14)
$sub-nav-font-color ?=                       $gray-dark-1
$sub-nav-font-weight ?=                      $font-weight-normal
$sub-nav-text-decoration ?=                  none
$sub-nav-padding ?=                          rem-calc(3 16)
$sub-nav-border-radius ?=                    $global-radius
$sub-nav-font-color-hover ?=                 darken($sub-nav-font-color, 25%)

// the active item styles
$sub-nav-active-font-weight ?=               $font-weight-normal
$sub-nav-active-bg ?=                        $primary-color
$sub-nav-active-bg-hover ?=                  darken($sub-nav-active-bg, 14%)
$sub-nav-active-color ?=                     $white
$sub-nav-active-padding ?=                   $sub-nav-padding
$sub-nav-active-cursor ?=                    default

$sub-nav-item-divider ?=                     ''
$sub-nav-item-divider-margin ?=              rem-calc(12)


//
// @mixins
//

// Create a sub-nav item
//
// $font-color - Font color. Default: $sub-nav-font-color.
// $font-size - Font size. Default: $sub-nav-font-size.
// $active-bg - Background of active nav item. Default: $sub-nav-active-bg.
// $active-bg-hover - Background of active nav item, when hovered. Default: $sub-nav-active-bg-hover.
sub-nav($font-color = $sub-nav-font-color, $font-size = $sub-nav-font-size, $active-bg = $sub-nav-active-bg, $active-bg-hover = $sub-nav-active-bg-hover) {
    display: block
    margin: $sub-nav-list-margin
    overflow: hidden
    padding-top: $sub-nav-list-padding-top
    width: auto

    dt {
        text-transform: uppercase
    }

    dt,
    dd,
    li {
        color: $font-color
        float: $default-float
        font-size: $font-size
        fontweight($sub-nav-font-weight)
        font_family($sub-nav-font-family)
        margin-bottom: 0
        margin-{$default-float}: rem-calc(16)

        a {
            color: $sub-nav-font-color
            padding: $sub-nav-padding
            text-decoration: $sub-nav-text-decoration

            &:hover {
                color: $sub-nav-font-color-hover
            }
        }

        &.is-active a {
            background: $active-bg
            color: $sub-nav-active-color
            cursor: $sub-nav-active-cursor
            fontweight($sub-nav-active-font-weight)
            padding: $sub-nav-active-padding
            radius($sub-nav-border-radius)

            &:hover {
                background: $active-bg-hover
            }
        }
        if ($sub-nav-item-divider != '') {
            margin-{$default-float}: 0

            &:before {
                content: $sub-nav-item-divider
                margin: 0 $sub-nav-item-divider-margin
            }

            &:first-child:before {
                content: ''
                margin: 0
            }
        }
    }
}

//
// @styles
//

if $include-html-sub-nav-classes {
    .sub-nav {
        sub-nav()
    }
}